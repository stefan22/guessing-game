/**
 *  Messages Reducer
 * @param {array} state
 * @param {object} action
 * @returns {boolean}
 */

const initialState = {
  gameRules: false
}

export const messagesReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
