/**
 * GuessedWords Reducer
 * @param {array} state
 * @param {object} action
 * @returns {array}
 */

const initialState = {
  guessedWords: []
}

export const guessedWordsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
