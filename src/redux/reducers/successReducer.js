/**
 * Success Reducer
 * @param {array} state
 * @param {object} action
 * @returns {boolean}
 */

const initialState = {
  success: false
}

export const successReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
