/**
 *  Messages Reducer
 * @param {array} state
 * @param {object} action
 * @returns {array} messages
 */

const messagesReducer = (state = [], action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default messagesReducer
