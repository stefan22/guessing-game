/**
 * WordsGuess Reducer
 * @param {array} state
 * @param {object} action
 * @returns {array}
 */

const wordsGuessedReducer = (state = [], action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default wordsGuessedReducer
