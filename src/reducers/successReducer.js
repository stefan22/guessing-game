/**
 * Success Reducer
 * @param {array} state
 * @param {object} action
 * @returns {boolean}
 */

const successReducer = (state = [], action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default successReducer
